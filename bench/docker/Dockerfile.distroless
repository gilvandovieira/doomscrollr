# distroless/cc — glibc, tiny, no shell. The "small + actually works" alternative to Alpine.
FROM doomscrollr-api:compiled AS bin
FROM gcr.io/distroless/cc-debian12
COPY --from=bin /app/server /app/server
ENV APP_ENV=production
EXPOSE 8000
CMD ["/app/server"]
